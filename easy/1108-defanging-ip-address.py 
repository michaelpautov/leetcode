class Solution(object):
    def defangIPaddr(self, address):
        res = ''
        for c in address:
            if c == '.':
                res += '[.]'
            else:
                res += c
        return res
        """
        :type address: str
        :rtype: str
        """
