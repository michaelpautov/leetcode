class Solution(object):
    def removeVowels(self, S):
        """
        :type S: str
        :rtype: str
        """
        res = ''
        for c in S:
            if c != 'a' and c != 'e' and c != 'i' and c != 'u' and c != 'o':
                res += c
        return res

